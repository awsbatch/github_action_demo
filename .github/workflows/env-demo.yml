name: Env demo
on: workflow_dispatch
env:
  APP_NAME: "dev"
jobs:
  env_demo:
    runs-on: ubuntu-latest
    steps:
      - name: Connecting to env
        run: echo "App is connected with $APP_NAME"
        
