name: Env demo
on: workflow_dispatch
env:
  APP_NAME: "dev"
jobs:
  env_demo:
    env:   # check president with one take prirority workspace lever or job level
       APP_NAME: "test"
    runs-on: ubuntu-latest
    steps:
      - name: Connecting to env
        env:  # check on step level
          APP_NAME: "prod"
        run: echo "App is connected with $APP_NAME"
        
