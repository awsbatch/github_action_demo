# run multiple jobs in single workflow
name: Github Action Demo
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: print index.html file
        run: cat index.html
